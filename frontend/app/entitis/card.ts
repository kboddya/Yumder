export type card = {
    id?: number,
    name: string,
    ingredients?: string,
    instructions?: string,
    time_consumed?: string,
    energy_value_score?: string,
    url_to_picture?: string,
}